# Deployment instructions for PHP backend

Place the contents of this php/ directory on a PHP server (Apache + PHP 7.4+ recommended) and import the SQL file located in php/database/alliance_amour.sql. Configure php/config/database.php with your DB credentials.

Files included in php/ are the application source. GitHub Pages will not execute PHP.
